const defaultState = {
    users: [],
    selected: null
}

export const usersReducer = (state =  defaultState, action) => {

    switch(action.type) {

        default:
            return state;
    }
}